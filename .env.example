# Root .env — used by docker-compose to pass build-time args to frontend
# Copy to .env and fill in your values

# Clerk publishable key (dashboard.clerk.com → API Keys)
VITE_CLERK_PUBLISHABLE_KEY=pk_test_your-clerk-publishable-key-here
